using ASCII_DAG.Models;

namespace ASCII_DAG
{
    public static class Serialization
    {
        public static string? Serialize(Node lastNode)
        {
            //if (lastNode.NodePrereqs.Count == 0)
            //{
            //    return lastNode?.Name;
            //}
            //else
            //{
            //    ProcessPrereqNodes(lastNode.NodePrereqs);
            //    return "";
            //}
            return "";
        }

        //private ProcessPrereqNodes(List<Node> nodes)
        //{
        //    if (nodes != null && nodes.Count > 0)
        //    {
        //        foreach (Node node in nodes)
        //        {

        //        }
        //    }
        //}
    }
}
